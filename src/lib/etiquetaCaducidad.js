/**
 * Texto de anaquel para precio especial por caducidad.
 * La caducidad es obligatoria en la etiqueta.
 */
const { textoEtiquetaPrecioEspecial } = require("./descuentoCaducidad");

module.exports = { textoEtiquetaPrecioEspecial };
